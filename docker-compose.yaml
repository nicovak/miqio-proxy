services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      secrets:
        - github
    command: ["./miqio-proxy", "server"]
    env_file:
      - .env
    ports:
      - "8080:8080"
secrets:
  github:
    file: .env
